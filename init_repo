#!/usr/bin/env sh

git submodule init
git submodule update

git submodule foreach git submodule init
git submodule foreach git submodule update

cp ./snippets/support_functions.vim ./bundle/snipmate/plugin
rm -r ./bundle/snipmate/snippets

# Build command-t 
cd ~/.vim/bundle/command-t/
rake make
